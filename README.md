Sử dụng thư viện AXIOS call api.

SET là một hàm => thực hiện việc thay đổi giá trị cũ bằng giá trị mới

GET request: call api nhận về dữ liệu từ server

POST request: call api gửi đi dữ liệu cho server

